const testuserRequest = {};
const testuserMessageText = {};

async function testHandlerText(msg) {
  // console.log("testHandlerText");
  // console.log(msg);
  // console.log("testHandlerText");
  // if (/^\//g.test(msg.text) && !/\/\b[Ss]tart/g.test(msg.text)) {
  //   userId = msg.from.id;
  //   testuserMessageText[userId] = { text: msg.text };
  //   testuserRequest[userId] = {
  //     data: "await getProductCalories(`desc=${dishFromMessage}`)",
  //   };
  // }
}

module.exports = { testHandlerText };
